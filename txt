<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BabyCradle Pro | Premium Monitoring Dashboard</title>
    <script src="https://www.gstatic.com/firebasejs/8.10.1/firebase-app.js"></script>
    <script src="https://www.gstatic.com/firebasejs/8.10.1/firebase-database.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #4361ee;
            --primary-dark: #3a56d4;
            --secondary: #7209b7;
            --success: #4cc9f0;
            --warning: #f72585;
            --danger: #e63946;
            --info: #4895ef;
            --light: #f8f9fa;
            --dark: #212529;
            --gray-100: #f8f9fa;
            --gray-200: #e9ecef;
            --gray-300: #dee2e6;
            --gray-400: #ced4da;
            --gray-500: #adb5bd;
            --gray-600: #6c757d;
            --gray-700: #495057;
            --gray-800: #343a40;
            --gray-900: #212529;
            --gradient-primary: linear-gradient(135deg, #4361ee 0%, #3a0ca3 100%);
            --gradient-secondary: linear-gradient(135deg, #7209b7 0%, #560bad 100%);
            --gradient-success: linear-gradient(135deg, #4cc9f0 0%, #4895ef 100%);
            --gradient-warning: linear-gradient(135deg, #f72585 0%, #b5179e 100%);
            --gradient-danger: linear-gradient(135deg, #e63946 0%, #d00000 100%);
            --card-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
            --card-shadow-hover: 0 20px 40px rgba(0, 0, 0, 0.12);
            --transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', sans-serif;
        }

        body {
            background: linear-gradient(135deg, #f5f7fa 0%, #e4e8f0 100%);
            min-height: 100vh;
            color: var(--gray-800);
            padding: 0;
            overflow-x: hidden;
        }

        .container {
            max-width: 1800px;
            margin: 0 auto;
            padding: 20px;
        }

        /* Header Styles */
        .header {
            background: white;
            border-radius: 20px;
            padding: 25px 30px;
            margin-bottom: 25px;
            box-shadow: var(--card-shadow);
            display: flex;
            justify-content: space-between;
            align-items: center;
            animation: slideDown 0.6s ease;
            position: relative;
            overflow: hidden;
        }

        .header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 8px;
            height: 100%;
            background: var(--gradient-primary);
        }

        .header-content h1 {
            font-size: 2.4rem;
            font-weight: 700;
            color: var(--gray-900);
            margin-bottom: 5px;
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .header-content p {
            color: var(--gray-600);
            font-size: 1.1rem;
            font-weight: 400;
        }

        .status-badge {
            background: var(--gradient-primary);
            color: white;
            padding: 12px 25px;
            border-radius: 50px;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 10px;
            box-shadow: 0 5px 15px rgba(67, 97, 238, 0.3);
            transition: var(--transition);
        }

        .status-badge:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(67, 97, 238, 0.4);
        }

        .status-badge i {
            font-size: 1.2rem;
        }

        /* Dashboard Grid */
        .dashboard-grid {
            display: grid;
            grid-template-columns: 1fr 1fr 1fr;
            gap: 25px;
            margin-bottom: 25px;
        }

        @media (max-width: 1400px) {
            .dashboard-grid {
                grid-template-columns: 1fr 1fr;
            }
        }

        @media (max-width: 768px) {
            .dashboard-grid {
                grid-template-columns: 1fr;
            }
        }

        /* Card Styles */
        .card {
            background: white;
            border-radius: 20px;
            padding: 25px;
            box-shadow: var(--card-shadow);
            transition: var(--transition);
            animation: fadeIn 0.8s ease;
            position: relative;
            overflow: hidden;
        }

        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: var(--gradient-primary);
        }

        .card:hover {
            transform: translateY(-8px);
            box-shadow: var(--card-shadow-hover);
        }

        .card-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 15px;
            border-bottom: 1px solid var(--gray-200);
        }

        .card-title {
            font-size: 1.4rem;
            font-weight: 700;
            color: var(--gray-900);
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .card-title i {
            color: var(--primary);
            font-size: 1.6rem;
            background: var(--gradient-primary);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        /* Sensor Grid */
        .sensor-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
            gap: 20px;
            margin-bottom: 25px;
        }

        .sensor-card {
            background: white;
            color: var(--gray-800);
            padding: 25px 20px;
            border-radius: 15px;
            text-align: center;
            transition: var(--transition);
            position: relative;
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
            border: 1px solid var(--gray-200);
        }

        .sensor-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: var(--gradient-primary);
        }

        .sensor-card.critical {
            border-left: 4px solid var(--danger);
        }

        .sensor-card.warning {
            border-left: 4px solid var(--warning);
        }

        .sensor-card.success {
            border-left: 4px solid var(--success);
        }

        .sensor-icon {
            font-size: 2.5rem;
            margin-bottom: 15px;
            background: var(--gradient-primary);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .sensor-label {
            font-size: 0.9rem;
            color: var(--gray-600);
            margin-bottom: 8px;
            font-weight: 500;
        }

        .sensor-value {
            font-size: 2.2rem;
            font-weight: 700;
            margin: 10px 0;
            color: var(--gray-900);
        }

        .sensor-unit {
            font-size: 0.9rem;
            color: var(--gray-500);
        }

        /* Diaper Status */
        .diaper-status {
            text-align: center;
            padding: 20px;
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            border-radius: 15px;
            margin-bottom: 20px;
        }

        .diaper-emoji {
            font-size: 4rem;
            margin-bottom: 15px;
        }

        .diaper-condition {
            font-size: 1.8rem;
            font-weight: 700;
            margin-bottom: 10px;
            color: var(--gray-900);
        }

        .diaper-description {
            color: var(--gray-600);
            font-size: 1rem;
        }

        /* Fan Control */
        .fan-control {
            text-align: center;
        }

        .fan-speed-display {
            font-size: 3.5rem;
            font-weight: 700;
            color: var(--primary);
            margin: 20px 0;
            background: var(--gradient-primary);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .speed-slider-container {
            margin: 25px 0;
            padding: 0 10px;
        }

        .speed-slider {
            width: 100%;
            -webkit-appearance: none;
            height: 12px;
            border-radius: 10px;
            background: linear-gradient(to right, #4cc9f0, #4895ef, #4361ee, #3a0ca3);
            outline: none;
        }

        .speed-slider::-webkit-slider-thumb {
            -webkit-appearance: none;
            width: 28px;
            height: 28px;
            border-radius: 50%;
            background: white;
            border: 3px solid var(--primary);
            cursor: pointer;
            box-shadow: 0 3px 10px rgba(0,0,0,0.2);
            transition: var(--transition);
        }

        .speed-slider::-webkit-slider-thumb:hover {
            transform: scale(1.1);
        }

        .control-buttons {
            display: flex;
            gap: 12px;
            margin-top: 20px;
        }

        .btn {
            flex: 1;
            padding: 14px 20px;
            border: none;
            border-radius: 12px;
            font-weight: 600;
            cursor: pointer;
            transition: var(--transition);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
        }

        .btn-primary {
            background: var(--gradient-primary);
            color: white;
        }

        .btn-outline {
            background: transparent;
            border: 2px solid var(--primary);
            color: var(--primary);
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }

        .btn-primary:hover {
            background: var(--primary-dark);
        }

        .btn-outline:hover {
            background: var(--primary);
            color: white;
        }

        /* Charts */
        .chart-container {
            position: relative;
            height: 300px;
            width: 100%;
            margin-top: 15px;
        }

        /* Alerts */
        .alert-panel {
            background: #fff9e6;
            border-radius: 15px;
            padding: 20px;
            margin-top: 20px;
            border-left: 5px solid var(--warning);
        }

        .alert {
            padding: 18px 20px;
            margin: 12px 0;
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            animation: slideIn 0.5s ease;
            background: white;
            box-shadow: 0 3px 10px rgba(0,0,0,0.08);
            transition: var(--transition);
        }

        .alert:hover {
            transform: translateX(5px);
        }

        .alert.cry { 
            border-left: 5px solid var(--danger);
            background: linear-gradient(to right, #ffeaea, white);
        }
        
        .alert.fever { 
            border-left: 5px solid var(--warning);
            background: linear-gradient(to right, #fff0e6, white);
        }
        
        .alert.moisture { 
            border-left: 5px solid var(--info);
            background: linear-gradient(to right, #e6f7ff, white);
        }

        .alert-content {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .alert-icon {
            font-size: 1.8rem;
            width: 50px;
            text-align: center;
        }

        .alert-message {
            font-weight: 600;
            color: var(--gray-900);
        }

        .alert-time {
            color: var(--gray-600);
            font-size: 0.9rem;
        }

        /* Stats Grid */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-top: 25px;
        }

        .stat-card {
            background: white;
            padding: 20px;
            border-radius: 15px;
            text-align: center;
            box-shadow: 0 5px 15px rgba(0,0,0,0.08);
            transition: var(--transition);
            border: 1px solid var(--gray-200);
        }

        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.1);
        }

        .stat-value {
            font-size: 2.5rem;
            font-weight: 700;
            margin: 10px 0;
            color: var(--gray-900);
        }

        .stat-label {
            color: var(--gray-600);
            font-size: 0.9rem;
        }

        /* Animations */
        @keyframes slideDown {
            from { transform: translateY(-30px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        @keyframes slideIn {
            from { transform: translateX(-20px); opacity: 0; }
            to { transform: translateX(0); opacity: 1; }
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }

        .pulse {
            animation: pulse 2s infinite;
        }

        /* Toggle Switch */
        .toggle-container {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 15px;
        }

        .toggle-text {
            font-weight: 500;
            color: var(--gray-700);
        }

        .toggle-switch {
            position: relative;
            display: inline-block;
            width: 60px;
            height: 30px;
        }

        .toggle-switch input {
            opacity: 0;
            width: 0;
            height: 0;
        }

        .slider {
            position: absolute;
            cursor: pointer;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: var(--gray-400);
            transition: .4s;
            border-radius: 34px;
        }

        .slider:before {
            position: absolute;
            content: "";
            height: 22px;
            width: 22px;
            left: 4px;
            bottom: 4px;
            background-color: white;
            transition: .4s;
            border-radius: 50%;
        }

        input:checked + .slider {
            background: var(--gradient-primary);
        }

        input:checked + .slider:before {
            transform: translateX(30px);
        }

        /* Full-width card */
        .full-width {
            grid-column: 1 / -1;
        }

        /* Progress Bar */
        .progress-container {
            margin: 15px 0;
        }

        .progress-label {
            display: flex;
            justify-content: space-between;
            margin-bottom: 5px;
            font-size: 0.9rem;
            color: var(--gray-700);
        }

        .progress-bar {
            height: 10px;
            background-color: var(--gray-200);
            border-radius: 5px;
            overflow: hidden;
        }

        .progress-fill {
            height: 100%;
            background: var(--gradient-primary);
            border-radius: 5px;
            transition: width 0.5s ease;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <div class="header-content">
                <h1><i class="fas fa-baby"></i> BabyCradle Pro</h1>
                <p>Premium Smart Monitoring System</p>
            </div>
            <div class="status-badge" id="connectionStatus">
                <i class="fas fa-circle"></i>
                <span>Connecting...</span>
            </div>
        </div>

        <!-- Main Dashboard Grid -->
        <div class="dashboard-grid">
            <!-- Vital Signs -->
            <div class="card">
                <div class="card-header">
                    <h2 class="card-title"><i class="fas fa-heartbeat"></i> Vital Signs</h2>
                </div>
                <div class="sensor-grid">
                    <div class="sensor-card" id="babyTempCard">
                        <div class="sensor-icon"><i class="fas fa-thermometer-half"></i></div>
                        <div class="sensor-label">Baby Temperature</div>
                        <div class="sensor-value" id="babyTemp">--</div>
                        <div class="sensor-unit">°C</div>
                    </div>
                    <div class="sensor-card" id="roomTempCard">
                        <div class="sensor-icon"><i class="fas fa-temperature-low"></i></div>
                        <div class="sensor-label">Room Temperature</div>
                        <div class="sensor-value" id="roomTemp">--</div>
                        <div class="sensor-unit">°C</div>
                    </div>
                    <div class="sensor-card">
                        <div class="sensor-icon"><i class="fas fa-tint"></i></div>
                        <div class="sensor-label">Humidity</div>
                        <div class="sensor-value" id="roomHum">--</div>
                        <div class="sensor-unit">%</div>
                    </div>
                </div>
            </div>

            <!-- Diaper Status -->
            <div class="card">
                <div class="card-header">
                    <h2 class="card-title"><i class="fas fa-baby-carriage"></i> Diaper Status</h2>
                </div>
                <div class="diaper-status">
                    <div class="diaper-emoji" id="diaperEmoji">✅</div>
                    <div class="diaper-condition" id="diaperCondition">Dry</div>
                    <div class="diaper-description" id="diaperDescription">Diaper is clean and dry</div>
                </div>
                <div class="progress-container">
                    <div class="progress-label">
                        <span>Moisture Level</span>
                        <span id="moisturePercent">0%</span>
                    </div>
                    <div class="progress-bar">
                        <div class="progress-fill" id="moistureProgress" style="width: 0%"></div>
                    </div>
                </div>
                <div class="stats-grid">
                    <div class="stat-card">
                        <div class="stat-label">Moisture Level</div>
                        <div class="stat-value" id="moistureValue">--</div>
                        <div class="stat-label">ADC Value</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-label">Sound Level</div>
                        <div class="stat-value" id="micValue">--</div>
                        <div class="stat-label">ADC Value</div>
                    </div>
                </div>
            </div>

            <!-- Climate Control -->
            <div class="card">
                <div class="card-header">
                    <h2 class="card-title"><i class="fas fa-fan"></i> Climate Control</h2>
                </div>
                <div class="fan-control">
                    <div class="toggle-container">
                        <span class="toggle-text">Auto Mode</span>
                        <label class="toggle-switch">
                            <input type="checkbox" id="autoModeToggle" checked>
                            <span class="slider"></span>
                        </label>
                    </div>
                    <div class="fan-speed-display" id="fanSpeedDisplay">0%</div>
                    <div class="control-mode" id="controlMode">Auto Mode: Active</div>
                    
                    <div class="speed-slider-container">
                        <input type="range" min="0" max="100" value="0" class="speed-slider" id="fanSlider">
                    </div>
                    
                    <div class="control-buttons">
                        <button class="btn btn-outline" onclick="setAutoMode(true)">
                            <i class="fas fa-robot"></i> Auto Mode
                        </button>
                        <button class="btn btn-primary" onclick="setFanSpeed()">
                            <i class="fas fa-paper-plane"></i> Apply
                        </button>
                    </div>
                </div>
            </div>

            <!-- Temperature Trends -->
            <div class="card full-width">
                <div class="card-header">
                    <h2 class="card-title"><i class="fas fa-chart-line"></i> Temperature Trends</h2>
                    <div class="time-filter">
                        <button class="btn btn-outline" style="padding: 8px 15px; font-size: 0.9rem;">1H</button>
                        <button class="btn btn-outline" style="padding: 8px 15px; font-size: 0.9rem;">6H</button>
                        <button class="btn btn-primary" style="padding: 8px 15px; font-size: 0.9rem;">24H</button>
                    </div>
                </div>
                <div class="chart-container">
                    <canvas id="tempChart"></canvas>
                </div>
            </div>

            <!-- Active Alerts -->
            <div class="card">
                <div class="card-header">
                    <h2 class="card-title"><i class="fas fa-bell"></i> Active Alerts</h2>
                    <span class="badge" id="alertCount" style="background: var(--danger); color: white; padding: 5px 10px; border-radius: 20px;">0</span>
                </div>
                <div id="activeAlerts">
                    <div class="alert" style="background: #e8f5e8; border-left: 5px solid #27ae60;">
                        <div class="alert-content">
                            <div class="alert-icon">✅</div>
                            <div>
                                <div class="alert-message">All systems normal</div>
                                <div class="alert-time">No active alerts</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- System Status -->
            <div class="card">
                <div class="card-header">
                    <h2 class="card-title"><i class="fas fa-cogs"></i> System Status</h2>
                </div>
                <div class="stats-grid">
                    <div class="stat-card">
                        <div class="stat-label">Uptime</div>
                        <div class="stat-value" id="uptime">--</div>
                        <div class="stat-label">Hours</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-label">Data Points</div>
                        <div class="stat-value" id="dataPoints">--</div>
                        <div class="stat-label">Today</div>
                    </div>
                </div>
                <div class="progress-container">
                    <div class="progress-label">
                        <span>System Load</span>
                        <span id="systemLoad">0%</span>
                    </div>
                    <div class="progress-bar">
                        <div class="progress-fill" id="systemLoadProgress" style="width: 0%"></div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Alert History -->
        <div class="card full-width">
            <div class="card-header">
                <h2 class="card-title"><i class="fas fa-history"></i> Alert History</h2>
                <button class="btn btn-outline" onclick="clearAlerts()">
                    <i class="fas fa-trash"></i> Clear History
                </button>
            </div>
            <div id="alertHistory" style="max-height: 400px; overflow-y: auto;">
                <p style="text-align: center; color: var(--gray-600); padding: 40px;">No alert history yet...</p>
            </div>
        </div>
    </div>

    <audio id="alertSound" preload="auto">
        <source src="https://assets.mixkit.co/active_storage/sfx/257/257-preview.mp3" type="audio/mpeg">
    </audio>

    <script>
        // Firebase configuration
        const firebaseConfig = {
            apiKey: "AIzaSyBv2fcGVOYlBORULLErZKqZ1WauSxRj6FI",
            authDomain: "smart-baby-cradle-b2f92.firebaseapp.com",
            databaseURL: "https://smart-baby-cradle-b2f92-default-rtdb.asia-southeast1.firebasedatabase.app",
            projectId: "smart-baby-cradle-b2f92",
            storageBucket: "smart-baby-cradle-b2f92.firebasestorage.app",
            messagingSenderId: "169217508469",
            appId: "1:169217508469:web:57cda5d9f07a8da760e43c"
        };

        // Initialize Firebase
        firebase.initializeApp(firebaseConfig);
        const database = firebase.database();

        // Chart initialization
        const tempChart = new Chart(document.getElementById('tempChart'), {
            type: 'line',
            data: {
                labels: [],
                datasets: [
                    {
                        label: 'Room Temperature (°C)',
                        data: [],
                        borderColor: '#4361ee',
                        backgroundColor: 'rgba(67, 97, 238, 0.1)',
                        borderWidth: 3,
                        tension: 0.4,
                        fill: true,
                        pointBackgroundColor: '#4361ee',
                        pointBorderColor: '#ffffff',
                        pointBorderWidth: 2,
                        pointRadius: 4,
                        pointHoverRadius: 6
                    },
                    {
                        label: 'Baby Temperature (°C)',
                        data: [],
                        borderColor: '#f72585',
                        backgroundColor: 'rgba(247, 37, 133, 0.1)',
                        borderWidth: 3,
                        tension: 0.4,
                        fill: true,
                        pointBackgroundColor: '#f72585',
                        pointBorderColor: '#ffffff',
                        pointBorderWidth: 2,
                        pointRadius: 4,
                        pointHoverRadius: 6
                    }
                ]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        position: 'top',
                        labels: {
                            usePointStyle: true,
                            padding: 20,
                            font: {
                                size: 13,
                                weight: '500'
                            }
                        }
                    },
                    title: {
                        display: true,
                        text: 'Temperature Monitoring',
                        font: {
                            size: 16,
                            weight: '600'
                        },
                        padding: 20
                    }
                },
                scales: {
                    y: {
                        beginAtZero: false,
                        title: {
                            display: true,
                            text: 'Temperature (°C)',
                            font: {
                                size: 13,
                                weight: '500'
                            }
                        },
                        grid: {
                            color: 'rgba(0, 0, 0, 0.05)'
                        }
                    },
                    x: {
                        title: {
                            display: true,
                            text: 'Time',
                            font: {
                                size: 13,
                                weight: '500'
                            }
                        },
                        grid: {
                            color: 'rgba(0, 0, 0, 0.05)'
                        }
                    }
                }
            }
        });

        let sensorDataHistory = [];
        let lastAlertCheck = 0;
        let isAutoMode = true;
        let startTime = Date.now();

        // Firebase listeners
        database.ref('sensorData/latest').on('value', (snapshot) => {
            const data = snapshot.val();
            if (data) {
                updateDashboard(data);
                updateConnectionStatus(true);
                
                // Store data for charts
                sensorDataHistory.push({
                    timestamp: new Date().toISOString(),
                    ...data
                });
                
                // Keep only last 50 records
                if (sensorDataHistory.length > 50) {
                    sensorDataHistory.shift();
                }
            }
        });

        database.ref('alerts').on('value', (snapshot) => {
            const alerts = snapshot.val();
            updateAlertHistory(alerts);
        });

        function updateDashboard(data) {
            // Update sensor values
            document.getElementById('roomTemp').textContent = data.roomTemperature?.toFixed(1) || '--';
            document.getElementById('roomHum').textContent = data.roomHumidity?.toFixed(1) || '--';
            document.getElementById('babyTemp').textContent = data.babyTemperature?.toFixed(1) || '--';
            document.getElementById('micValue').textContent = data.micValue || '--';
            document.getElementById('moistureValue').textContent = data.moistureValue || '--';
            
            // Update diaper status
            document.getElementById('diaperEmoji').textContent = data.diaperEmoji || '✅';
            document.getElementById('diaperCondition').textContent = data.diaperCondition || 'Dry';
            document.getElementById('diaperDescription').textContent = getDiaperDescription(data.diaperCondition);
            
            // Update moisture progress
            const moisturePercent = Math.max(0, 100 - ((data.moistureValue || 0) / 4095 * 100));
            document.getElementById('moisturePercent').textContent = Math.round(moisturePercent) + '%';
            document.getElementById('moistureProgress').style.width = moisturePercent + '%';
            
            // Update fan control
            document.getElementById('fanSpeedDisplay').textContent = (data.fanSpeed || 0) + '%';
            if (!isAutoMode) {
                document.getElementById('fanSlider').value = data.fanSpeed || 0;
            }
            document.getElementById('controlMode').textContent = data.autoFanControl ? 
                'Auto Mode: Active' : 'Manual Mode: Active';
            
            // Update system status
            const uptimeHours = ((Date.now() - startTime) / (1000 * 60 * 60)).toFixed(1);
            document.getElementById('uptime').textContent = uptimeHours;
            document.getElementById('dataPoints').textContent = sensorDataHistory.length;
            
            // Update system load (simulated)
            const systemLoad = Math.min(100, 20 + Math.random() * 30);
            document.getElementById('systemLoad').textContent = Math.round(systemLoad) + '%';
            document.getElementById('systemLoadProgress').style.width = systemLoad + '%';
            
            // Update card colors based on conditions
            updateCardColors(data);
            
            // Update charts
            updateChart(data);
            
            // Check alerts
            checkAlerts(data);
        }

        function getDiaperDescription(condition) {
            switch(condition) {
                case 'Dry': return 'Diaper is clean and dry';
                case 'Slightly Wet': return 'Diaper is slightly moist';
                case 'Wet': return 'Diaper needs changing';
                case 'Very Wet': return 'Diaper is very wet - change immediately';
                case 'Soaked': return 'Diaper is soaked - urgent change required';
                default: return 'Monitoring diaper condition';
            }
        }

        function updateCardColors(data) {
            const babyTempCard = document.getElementById('babyTempCard');
            const roomTempCard = document.getElementById('roomTempCard');
            
            // Baby temperature color coding
            if (data.babyTemperature > 37.5) {
                babyTempCard.className = 'sensor-card critical';
            } else if (data.babyTemperature > 37.0) {
                babyTempCard.className = 'sensor-card warning';
            } else {
                babyTempCard.className = 'sensor-card success';
            }
            
            // Room temperature color coding
            if (data.roomTemperature > 28) {
                roomTempCard.className = 'sensor-card warning';
            } else if (data.roomTemperature < 20) {
                roomTempCard.className = 'sensor-card warning';
            } else {
                roomTempCard.className = 'sensor-card success';
            }
        }

        function updateChart(data) {
            const now = new Date().toLocaleTimeString();
            
            // Add new data point
            tempChart.data.labels.push(now);
            tempChart.data.datasets[0].data.push(data.roomTemperature);
            tempChart.data.datasets[1].data.push(data.babyTemperature);
            
            // Keep only last 15 data points
            if (tempChart.data.labels.length > 15) {
                tempChart.data.labels.shift();
                tempChart.data.datasets[0].data.shift();
                tempChart.data.datasets[1].data.shift();
            }
            
            tempChart.update();
        }

        function checkAlerts(data) {
            const alertsContainer = document.getElementById('activeAlerts');
            let activeAlerts = [];
            
            if (data.isCrying) {
                activeAlerts.push({
                    type: 'CRYING',
                    message: 'Baby is crying - Auto swing activated',
                    class: 'cry',
                    icon: '😢'
                });
            }
            
            if (data.hasFever) {
                activeAlerts.push({
                    type: 'FEVER',
                    message: `High temperature detected: ${data.babyTemperature}°C`,
                    class: 'fever',
                    icon: '🌡️'
                });
            }
            
            if (data.isWet) {
                activeAlerts.push({
                    type: 'MOISTURE',
                    message: `Diaper condition: ${data.diaperCondition}`,
                    class: 'moisture',
                    icon: data.diaperEmoji || '💧'
                });
            }
            
            // Update alert count
            document.getElementById('alertCount').textContent = activeAlerts.length;
            
            if (activeAlerts.length === 0) {
                alertsContainer.innerHTML = `
                    <div class="alert" style="background: #e8f5e8; border-left: 5px solid #27ae60;">
                        <div class="alert-content">
                            <div class="alert-icon">✅</div>
                            <div>
                                <div class="alert-message">All systems normal</div>
                                <div class="alert-time">No active alerts</div>
                            </div>
                        </div>
                    </div>
                `;
            } else {
                alertsContainer.innerHTML = activeAlerts.map(alert => `
                    <div class="alert ${alert.class} pulse">
                        <div class="alert-content">
                            <div class="alert-icon">${alert.icon}</div>
                            <div>
                                <div class="alert-message">${alert.message}</div>
                                <div class="alert-time">${new Date().toLocaleTimeString()}</div>
                            </div>
                        </div>
                    </div>
                `).join('');
                
                // Play alert sound for critical alerts (throttled)
                const now = Date.now();
                if ((now - lastAlertCheck > 10000) && (data.isCrying || data.hasFever)) {
                    playAlertSound();
                    lastAlertCheck = now;
                }
            }
        }

        function updateAlertHistory(alerts) {
            const historyContainer = document.getElementById('alertHistory');
            
            if (!alerts) {
                historyContainer.innerHTML = '<p style="text-align: center; color: var(--gray-600); padding: 40px;">No alert history yet...</p>';
                return;
            }
            
            const alertList = Object.values(alerts).reverse();
            
            historyContainer.innerHTML = alertList.map(alert => `
                <div class="alert ${alert.type.toLowerCase()}">
                    <div class="alert-content">
                        <div class="alert-icon">${getAlertIcon(alert.type)}</div>
                        <div>
                            <div class="alert-message">${alert.message || getDefaultAlertMessage(alert)}</div>
                            <div class="alert-time">${new Date(alert.timestamp).toLocaleString()}</div>
                        </div>
                    </div>
                </div>
            `).join('');
        }

        function getAlertIcon(type) {
            switch(type) {
                case 'CRYING': return '😢';
                case 'FEVER': return '🌡️';
                case 'MOISTURE': return '💧';
                default: return '⚠️';
            }
        }

        function getDefaultAlertMessage(alert) {
            switch(alert.type) {
                case 'CRYING': return `Cry detected (Level: ${alert.value})`;
                case 'FEVER': return `High temperature: ${alert.value}°C`;
                case 'MOISTURE': return `Diaper ${alert.condition || 'wet'}`;
                default: return alert.value;
            }
        }

        function playAlertSound() {
            const alertSound = document.getElementById('alertSound');
            alertSound.play().catch(e => console.log('Audio play failed:', e));
        }

        function updateConnectionStatus(connected) {
            const statusElem = document.getElementById('connectionStatus');
            const icon = statusElem.querySelector('i');
            const text = statusElem.querySelector('span');
            
            if (connected) {
                statusElem.style.background = 'var(--gradient-primary)';
                icon.className = 'fas fa-circle';
                text.textContent = 'System Online';
            } else {
                statusElem.style.background = 'var(--gradient-danger)';
                icon.className = 'fas fa-circle';
                text.textContent = 'System Offline';
            }
        }

        // Control Functions
        function setFanSpeed() {
            const speed = document.getElementById('fanSlider').value;
            isAutoMode = false;
            
            database.ref('controls').update({
                fanSpeed: parseInt(speed),
                autoMode: false
            });
            
            document.getElementById('controlMode').textContent = 'Manual Mode: Active';
        }

        function setAutoMode(enabled) {
            isAutoMode = enabled;
            
            database.ref('controls/autoMode').set(enabled);
            
            document.getElementById('controlMode').textContent = enabled ? 
                'Auto Mode: Active' : 'Manual Mode: Active';
            
            if (enabled) {
                document.getElementById('fanSlider').value = 0;
            }
        }

        function clearAlerts() {
            if (confirm('Are you sure you want to clear all alert history?')) {
                database.ref('alerts').remove();
            }
        }

        // Initialize
        setTimeout(() => updateConnectionStatus(true), 2000);
        
        // Toggle switch event listener
        document.getElementById('autoModeToggle').addEventListener('change', function() {
            setAutoMode(this.checked);
        });
    </script>
</body>
</html>
